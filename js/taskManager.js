// updated and used console log to make sure task manager works

class TaskManager {
    tasks;
    constructor(myTasks) {
        this.tasks = "";

    const currentId = [0, 1, 2, 3, 4];
    this.currentId = currentId;

    };
};


// created an array to connect with the task objects
